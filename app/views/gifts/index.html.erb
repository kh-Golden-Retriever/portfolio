<div class="container">
  <div class="mt-5 mb-4">
    <nav class="navbar nav-tabs pb-0">
      <ul class="nav mr-auto">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Active</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Link</a>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
        </li>
      </ul>
      <ul class="list-unstyled">
        <li class="nav-item">
          <%= render 'shared/gift_search_form', url: gifts_path %>
        </li>
      </ul>
    </nav>
  </div>
  <div class="row g-4">
    <% if @gifts.present? %>
      <%= render @gifts %>
    <% else %>
      <p>No gifts</p>
    <% end %>
  </div>
</div>
