<div class="container mt-5">
  <div class="row gx-0 min-vh-100">
    <div class="col-2">
      <%= render 'show_images', gift: @gift %>
    </div>
    <div class="col-6">
      <%= render 'show_detail',{ gift: @gift, users: @users }%>
    </div>
    <div class="col-4">
      <div>
        <%= render 'comments/comments',  {gift: @gift, comments: @comments}  %>
      </div>
      <div>
        <%= render 'comments/form', { gift: @gift, comment: @comment }%>
      </div>
    </div>
  </div>
</div>
